# Generate SECRET_KEY with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-string

# MySQL
MYSQL_ROOT_PASSWORD=change-this-root-password
DB_NAME=vgcto
DB_USER=vgcto
DB_PASS=change-this-db-password

# Admin login
ADMIN_USERNAME=admin
# Generate hash with: python3 -c "import hashlib; print(hashlib.sha256('yourpassword'.encode()).hexdigest())"
ADMIN_PASSWORD_HASH=change-this-to-hashed-password
